version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: distributed-llm-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: distributed_llm
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - distributed-llm-network

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: distributed-llm-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - distributed-llm-network

  # Central Router
  router:
    build:
      context: .
      dockerfile: docker/Dockerfile.router
    container_name: distributed-llm-router
    environment:
      - DATABASE_URL=postgresql://postgres:password@postgres:5432/distributed_llm
      - REDIS_URL=redis://redis:6379/0
      - HOST=0.0.0.0
      - PORT=8000
      - LOG_LEVEL=info
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - distributed-llm-network

  # Workflow Worker (will be implemented later with Rust async tasks)
  # celery-worker:
  #   Commented out - will be replaced with Rust async task worker

  # Provider Agent (Example - can be scaled)
  provider-01:
    build:
      context: .
      dockerfile: docker/Dockerfile.provider
    container_name: distributed-llm-provider-01
    environment:
      - PROVIDER_ID=provider_001
      - PROVIDER_NAME=Provider 01
      - ROUTER_URL=http://router:8000
      - VLLM_HOST=localhost
      - VLLM_PORT=8001
      - HOST=0.0.0.0
      - PORT=8001
      - LOG_LEVEL=info
      - DATABASE_URL=postgresql://postgres:password@postgres:5432/distributed_llm
      - REDIS_URL=redis://redis:6379/0
    env_file:
      - .env
    ports:
      - "8001:8001"
    depends_on:
      - router
    networks:
      - distributed-llm-network

  # Prometheus (Monitoring)
  prometheus:
    image: prom/prometheus:latest
    container_name: distributed-llm-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./docker/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    networks:
      - distributed-llm-network

  # Grafana (Dashboards)
  grafana:
    image: grafana/grafana:latest
    container_name: distributed-llm-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - distributed-llm-network

volumes:
  postgres_data:
  redis_data:
  prometheus_data:
  grafana_data:

networks:
  distributed-llm-network:
    driver: bridge
